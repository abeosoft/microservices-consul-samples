server:
  port: 8080

spring:
  application:
    name: news-service
  cloud:
    consul:
      host: localhost
      port: 8500    
      discovery:
        healthCheckInterval: 30s
      config:
        enabled: true    